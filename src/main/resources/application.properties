spring.application.name=hrms360
server.port=8183

spring.datasource.url=jdbc:mysql://localhost:3306/hrms360
spring.datasource.username=root
spring.datasource.password=Mysql@1212
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Log pattern (Note: Timestamp is controlled via the logback configuration, but other elements can be set here)
#logging.pattern.console=%d{MM-yyyy-dd HH:mm:ss} - %msg%n